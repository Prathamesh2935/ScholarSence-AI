# ScholarSense AI — Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
# Mirrored placeholders (backend is source of truth for secrets).
# Do NOT expose real secrets with NEXT_PUBLIC_ prefix in production.
OPENAI_API_KEY=
QDRANT_HOST=localhost
QDRANT_API_KEY=
