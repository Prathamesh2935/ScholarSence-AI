# ScholarSense AI — production backend dependencies.
# Kept slim on purpose: Vercel serverless functions cap at 500 MB.
# Heavy/optional extras (unstructured, torch) are NOT listed — the code
# degrades gracefully when they are absent (PyMuPDF-only parsing,
# hash embeddings, built-in BM25, in-memory index).
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.7.0
pymupdf>=1.24.0
qdrant-client>=1.9.0
openai>=1.40.0
python-multipart>=0.0.18
httpx>=0.27.0
python-dotenv>=1.0.1
rank-bm25>=0.2.2
